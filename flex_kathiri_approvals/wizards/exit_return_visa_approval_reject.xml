<odoo>
    <data>
        <!-- Define the view for the exit return visa rejection wizard -->
        <record id="view_flex_approval_exit_return_visa_reject_wizard_form" model="ir.ui.view">
            <field name="name">flex.approval.exit_return_visa.reject.wizard.form</field>
            <field name="model">flex.approval.exit_return_visa.reject.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <field name="reason" widget="text" placeholder="Enter the rejection reason..." required="1"/>
                    <footer>
                        <button string="Confirm Rejection" type="object" name="action_confirm_rejection"
                                class="oe_highlight"/>
                        <button string="Cancel" class="oe_highlight" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Define the action for the exit return visa rejection wizard -->
        <record id="action_flex_approval_exit_return_visa_reject_wizard" model="ir.actions.act_window">
            <field name="name">Rejection Reason</field>
            <field name="res_model">flex.approval.exit_return_visa.reject.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_flex_approval_exit_return_visa_reject_wizard_form"/>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
