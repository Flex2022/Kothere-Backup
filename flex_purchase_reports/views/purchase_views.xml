<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_purchase_lines_tree_view" model="ir.ui.view">
        <field name="name">purchase.order.line.tree</field>
        <field name="model">purchase.order.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="order_id"/>
                <field name="product_id"/>
                <field name="partner_id"/>
                <field name="date_approve"/>
                <field name="product_qty"/>
                <field name="product_uom"/>
                <field name="price_unit"/>
                <field name="price_subtotal"/>
            </tree>
        </field>
    </record>

    <record id="view_purchase_lines_pivot_view" model="ir.ui.view">
        <field name="name">purchase.order.line.pivot</field>
        <field name="model">purchase.order.line</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="order_id"/>
                <field name="product_id"/>
                <field name="partner_id"/>
                <field name="date_approve"/>
                <field name="product_qty" type="measure"/>
                <field name="product_uom"/>
                <field name="price_unit" type="measure"/>
                <field name="price_subtotal" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="view_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="اتفاق العقد">
                    <group>
                        <field name="date_approve_hijri" invisible="1"/>
                        <field name="partner_presenter_id" options="{'no_create': True}" readonly="1"/>
                        <field name="first_party_wishes"/>
                        <field name="agreement_purpose"/>
                        <field name="agreement_expense"/>
                    </group>
                    <notebook>
                        <page string="Conditions">
                            <field name="agreement_condition_ids">
                                <tree editable="bottom">
                                    <field name="purchase_order_id" column_invisible="1"/>
                                    <field name="name"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </page>
            </xpath>

            <field name="user_id" position="after">
                <field name="user_ids" widget="many2many_tags"/>
            </field>

            <field name="user_id" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>

        </field>
    </record>

</odoo>