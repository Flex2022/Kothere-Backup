<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase.group_purchase_user" model="res.groups">
        <field name="name">User (Own Documents)</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="category_id" ref="base.module_category_inventory_purchase"/>
    </record>

    <record id="group_purchase_user_all_documents" model="res.groups">
        <field name="name">User (All documents)</field>
        <field name="implied_ids" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        <field name="category_id" ref="base.module_category_inventory_purchase"/>
    </record>

    <record id="purchase.group_purchase_manager" model="res.groups">
        <field name="name">Administrator</field>
        <field name="category_id" ref="base.module_category_inventory_purchase"/>
        <field name="implied_ids" eval="[(4, ref('group_purchase_user_all_documents'))]"/>
        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

    <!--    # Rules-->
    <record id="purchase_order_own_documents_rule" model="ir.rule">
        <field name="name">Own Purchase Orders</field>
        <field ref="purchase.model_purchase_order" name="model_id"/>
        <field name="domain_force">['|','|',('user_id','=',user.id),('user_ids','in',user.id),('user_id','=',False)]</field>
        <field name="groups" eval="[(4, ref('purchase.group_purchase_user'))]"/>
    </record>

    <record id="purchase_order_all_documents_rule" model="ir.rule">
        <field name="name">All Purchase Orders</field>
        <field ref="purchase.model_purchase_order" name="model_id"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('group_purchase_user_all_documents'))]"/>
    </record>


</odoo>

